/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.portal;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.NovabillingApiApiException;
import core.NovabillingApiException;
import core.NovabillingApiHttpResponse;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import errors.BadRequestError;
import errors.NotFoundError;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.Void;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.portal.requests.CreateCheckoutPortalRequest;
import resources.portal.requests.GetBillingPortalRequest;
import resources.portal.requests.GetInvoicesPortalRequest;
import resources.portal.requests.GetPaymentsPortalRequest;
import resources.portal.requests.GetSubscriptionsPortalRequest;
import types.CheckoutResponse;
import types.PaginatedInvoiceResponse;
import types.PaginatedPaymentResponse;
import types.SubscriptionResponse;

public class AsyncRawPortalClient {
  protected final ClientOptions clientOptions;

  public AsyncRawPortalClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns subscriptions, recent invoices, payments, and summary stats for a customer. Use this to render a billing dashboard for your end-users.
   */
  public CompletableFuture<NovabillingApiHttpResponse<Void>> getBilling(String externalId) {
    return getBilling(externalId,GetBillingPortalRequest.builder().build());
  }

  /**
   * Returns subscriptions, recent invoices, payments, and summary stats for a customer. Use this to render a billing dashboard for your end-users.
   */
  public CompletableFuture<NovabillingApiHttpResponse<Void>> getBilling(String externalId,
      RequestOptions requestOptions) {
    return getBilling(externalId,GetBillingPortalRequest.builder().build(),requestOptions);
  }

  /**
   * Returns subscriptions, recent invoices, payments, and summary stats for a customer. Use this to render a billing dashboard for your end-users.
   */
  public CompletableFuture<NovabillingApiHttpResponse<Void>> getBilling(String externalId,
      GetBillingPortalRequest request) {
    return getBilling(externalId,request,null);
  }

  /**
   * Returns subscriptions, recent invoices, payments, and summary stats for a customer. Use this to render a billing dashboard for your end-users.
   */
  public CompletableFuture<NovabillingApiHttpResponse<Void>> getBilling(String externalId,
      GetBillingPortalRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/portal/customers")
      .addPathSegment(externalId)
      .addPathSegments("billing");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<NovabillingApiHttpResponse<Void>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            if (response.isSuccessful()) {
              future.complete(new NovabillingApiHttpResponse<>(null, response));
              return;
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
              if (response.code() == 404) {
                future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Returns all subscriptions for the customer with plan details.
     */
    public CompletableFuture<NovabillingApiHttpResponse<List<SubscriptionResponse>>> getSubscriptions(
        String externalId) {
      return getSubscriptions(externalId,GetSubscriptionsPortalRequest.builder().build());
    }

    /**
     * Returns all subscriptions for the customer with plan details.
     */
    public CompletableFuture<NovabillingApiHttpResponse<List<SubscriptionResponse>>> getSubscriptions(
        String externalId, RequestOptions requestOptions) {
      return getSubscriptions(externalId,GetSubscriptionsPortalRequest.builder().build(),requestOptions);
    }

    /**
     * Returns all subscriptions for the customer with plan details.
     */
    public CompletableFuture<NovabillingApiHttpResponse<List<SubscriptionResponse>>> getSubscriptions(
        String externalId, GetSubscriptionsPortalRequest request) {
      return getSubscriptions(externalId,request,null);
    }

    /**
     * Returns all subscriptions for the customer with plan details.
     */
    public CompletableFuture<NovabillingApiHttpResponse<List<SubscriptionResponse>>> getSubscriptions(
        String externalId, GetSubscriptionsPortalRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/portal/customers")
        .addPathSegment(externalId)
        .addPathSegments("subscriptions");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<NovabillingApiHttpResponse<List<SubscriptionResponse>>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<List<SubscriptionResponse>>() {}), response));
                return;
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Returns a paginated list of invoices. Filter by status to show only pending invoices.
       */
      public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> getInvoices(
          String externalId) {
        return getInvoices(externalId,GetInvoicesPortalRequest.builder().build());
      }

      /**
       * Returns a paginated list of invoices. Filter by status to show only pending invoices.
       */
      public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> getInvoices(
          String externalId, RequestOptions requestOptions) {
        return getInvoices(externalId,GetInvoicesPortalRequest.builder().build(),requestOptions);
      }

      /**
       * Returns a paginated list of invoices. Filter by status to show only pending invoices.
       */
      public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> getInvoices(
          String externalId, GetInvoicesPortalRequest request) {
        return getInvoices(externalId,request,null);
      }

      /**
       * Returns a paginated list of invoices. Filter by status to show only pending invoices.
       */
      public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> getInvoices(
          String externalId, GetInvoicesPortalRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/portal/customers")
          .addPathSegment(externalId)
          .addPathSegments("invoices");if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
          }
          if (request.getPage().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
          }
          if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaginatedInvoiceResponse.class), response));
                  return;
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Initiates a payment session with the configured payment provider. Returns a checkout URL to redirect the customer to.
         */
        public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
            String externalId, String invoiceId) {
          return createCheckout(externalId,invoiceId,CreateCheckoutPortalRequest.builder().build());
        }

        /**
         * Initiates a payment session with the configured payment provider. Returns a checkout URL to redirect the customer to.
         */
        public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
            String externalId, String invoiceId, RequestOptions requestOptions) {
          return createCheckout(externalId,invoiceId,CreateCheckoutPortalRequest.builder().build(),requestOptions);
        }

        /**
         * Initiates a payment session with the configured payment provider. Returns a checkout URL to redirect the customer to.
         */
        public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
            String externalId, String invoiceId, CreateCheckoutPortalRequest request) {
          return createCheckout(externalId,invoiceId,request,null);
        }

        /**
         * Initiates a payment session with the configured payment provider. Returns a checkout URL to redirect the customer to.
         */
        public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
            String externalId, String invoiceId, CreateCheckoutPortalRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/portal/customers")
            .addPathSegment(externalId)
            .addPathSegments("invoices")
            .addPathSegment(invoiceId)
            .addPathSegments("checkout");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", RequestBody.create("", null))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutResponse.class), response));
                    return;
                  }
                  try {
                    if (response.code() == 400) {
                      future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Returns a paginated list of all payments made by the customer.
           */
          public CompletableFuture<NovabillingApiHttpResponse<PaginatedPaymentResponse>> getPayments(
              String externalId) {
            return getPayments(externalId,GetPaymentsPortalRequest.builder().build());
          }

          /**
           * Returns a paginated list of all payments made by the customer.
           */
          public CompletableFuture<NovabillingApiHttpResponse<PaginatedPaymentResponse>> getPayments(
              String externalId, RequestOptions requestOptions) {
            return getPayments(externalId,GetPaymentsPortalRequest.builder().build(),requestOptions);
          }

          /**
           * Returns a paginated list of all payments made by the customer.
           */
          public CompletableFuture<NovabillingApiHttpResponse<PaginatedPaymentResponse>> getPayments(
              String externalId, GetPaymentsPortalRequest request) {
            return getPayments(externalId,request,null);
          }

          /**
           * Returns a paginated list of all payments made by the customer.
           */
          public CompletableFuture<NovabillingApiHttpResponse<PaginatedPaymentResponse>> getPayments(
              String externalId, GetPaymentsPortalRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/portal/customers")
              .addPathSegment(externalId)
              .addPathSegments("payments");if (request.getPage().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
              }
              if (request.getLimit().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<NovabillingApiHttpResponse<PaginatedPaymentResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaginatedPaymentResponse.class), response));
                      return;
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                }
              });
              return future;
            }
          }
