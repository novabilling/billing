/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.paymentproviders;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import core.ClientOptions;
import core.MediaTypes;
import core.NovabillingApiApiException;
import core.NovabillingApiException;
import core.NovabillingApiHttpResponse;
import core.ObjectMappers;
import core.RequestOptions;
import errors.ConflictError;
import errors.NotFoundError;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.paymentproviders.requests.CreateProviderDto;
import resources.paymentproviders.requests.DeletePaymentProvidersRequest;
import resources.paymentproviders.requests.GetPaymentProvidersRequest;
import resources.paymentproviders.requests.TestConnectionPaymentProvidersRequest;
import resources.paymentproviders.requests.UpdateProviderDto;
import types.PaymentProviderResponse;
import types.ProviderTestResponse;

public class RawPaymentProvidersClient {
  protected final ClientOptions clientOptions;

  public RawPaymentProvidersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve all configured payment providers for the tenant. Credentials are never returned.
   */
  public NovabillingApiHttpResponse<List<PaymentProviderResponse>> list() {
    return list(null);
  }

  /**
   * Retrieve all configured payment providers for the tenant. Credentials are never returned.
   */
  public NovabillingApiHttpResponse<List<PaymentProviderResponse>> list(
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/payment-providers");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, new TypeReference<List<PaymentProviderResponse>>() {}), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new NovabillingApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Set up a payment provider (stripe, paystack, flutterwave, or mpesa) with encrypted credentials. The provider with the lowest priority number is used by default for checkout.
     */
    public NovabillingApiHttpResponse<PaymentProviderResponse> configure(
        CreateProviderDto request) {
      return configure(request,null);
    }

    /**
     * Set up a payment provider (stripe, paystack, flutterwave, or mpesa) with encrypted credentials. The provider with the lowest priority number is used by default for checkout.
     */
    public NovabillingApiHttpResponse<PaymentProviderResponse> configure(CreateProviderDto request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/payment-providers");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new NovabillingApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentProviderResponse.class), response);
          }
          try {
            if (response.code() == 409) {
              throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new NovabillingApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieve a specific payment provider configuration. Credentials are not included.
       */
      public NovabillingApiHttpResponse<PaymentProviderResponse> get(String id) {
        return get(id,GetPaymentProvidersRequest.builder().build());
      }

      /**
       * Retrieve a specific payment provider configuration. Credentials are not included.
       */
      public NovabillingApiHttpResponse<PaymentProviderResponse> get(String id,
          RequestOptions requestOptions) {
        return get(id,GetPaymentProvidersRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve a specific payment provider configuration. Credentials are not included.
       */
      public NovabillingApiHttpResponse<PaymentProviderResponse> get(String id,
          GetPaymentProvidersRequest request) {
        return get(id,request,null);
      }

      /**
       * Retrieve a specific payment provider configuration. Credentials are not included.
       */
      public NovabillingApiHttpResponse<PaymentProviderResponse> get(String id,
          GetPaymentProvidersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/payment-providers")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentProviderResponse.class), response);
            }
            try {
              if (response.code() == 404) {
                throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new NovabillingApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Remove a payment provider configuration. This does not affect existing payments.
         */
        public NovabillingApiHttpResponse<PaymentProviderResponse> delete(String id) {
          return delete(id,DeletePaymentProvidersRequest.builder().build());
        }

        /**
         * Remove a payment provider configuration. This does not affect existing payments.
         */
        public NovabillingApiHttpResponse<PaymentProviderResponse> delete(String id,
            RequestOptions requestOptions) {
          return delete(id,DeletePaymentProvidersRequest.builder().build(),requestOptions);
        }

        /**
         * Remove a payment provider configuration. This does not affect existing payments.
         */
        public NovabillingApiHttpResponse<PaymentProviderResponse> delete(String id,
            DeletePaymentProvidersRequest request) {
          return delete(id,request,null);
        }

        /**
         * Remove a payment provider configuration. This does not affect existing payments.
         */
        public NovabillingApiHttpResponse<PaymentProviderResponse> delete(String id,
            DeletePaymentProvidersRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/payment-providers")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentProviderResponse.class), response);
              }
              try {
                if (response.code() == 404) {
                  throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new NovabillingApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * Update provider settings such as active status, priority, or credentials.
           */
          public NovabillingApiHttpResponse<PaymentProviderResponse> update(String id) {
            return update(id,UpdateProviderDto.builder().build());
          }

          /**
           * Update provider settings such as active status, priority, or credentials.
           */
          public NovabillingApiHttpResponse<PaymentProviderResponse> update(String id,
              RequestOptions requestOptions) {
            return update(id,UpdateProviderDto.builder().build(),requestOptions);
          }

          /**
           * Update provider settings such as active status, priority, or credentials.
           */
          public NovabillingApiHttpResponse<PaymentProviderResponse> update(String id,
              UpdateProviderDto request) {
            return update(id,request,null);
          }

          /**
           * Update provider settings such as active status, priority, or credentials.
           */
          public NovabillingApiHttpResponse<PaymentProviderResponse> update(String id,
              UpdateProviderDto request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/payment-providers")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new NovabillingApiException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentProviderResponse.class), response);
                }
                try {
                  if (response.code() == 404) {
                    throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new NovabillingApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Verify that the provider credentials are valid by making a test API call to the provider.
             */
            public NovabillingApiHttpResponse<ProviderTestResponse> testConnection(String id) {
              return testConnection(id,TestConnectionPaymentProvidersRequest.builder().build());
            }

            /**
             * Verify that the provider credentials are valid by making a test API call to the provider.
             */
            public NovabillingApiHttpResponse<ProviderTestResponse> testConnection(String id,
                RequestOptions requestOptions) {
              return testConnection(id,TestConnectionPaymentProvidersRequest.builder().build(),requestOptions);
            }

            /**
             * Verify that the provider credentials are valid by making a test API call to the provider.
             */
            public NovabillingApiHttpResponse<ProviderTestResponse> testConnection(String id,
                TestConnectionPaymentProvidersRequest request) {
              return testConnection(id,request,null);
            }

            /**
             * Verify that the provider credentials are valid by making a test API call to the provider.
             */
            public NovabillingApiHttpResponse<ProviderTestResponse> testConnection(String id,
                TestConnectionPaymentProvidersRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/payment-providers")
                .addPathSegment(id)
                .addPathSegments("test");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", RequestBody.create("", null))
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ProviderTestResponse.class), response);
                  }
                  try {
                    if (response.code() == 404) {
                      throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new NovabillingApiException("Network error executing HTTP request", e);
                }
              }
            }
