/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.analytics;

import core.ClientOptions;
import core.NovabillingApiApiException;
import core.NovabillingApiException;
import core.NovabillingApiHttpResponse;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import resources.analytics.requests.GetChurnCohortsAnalyticsRequest;
import resources.analytics.requests.GetCustomersAnalyticsRequest;
import resources.analytics.requests.GetMrrBreakdownAnalyticsRequest;
import resources.analytics.requests.GetNetRevenueAnalyticsRequest;
import resources.analytics.requests.GetPaymentsAnalyticsRequest;
import resources.analytics.requests.GetRevenueAnalyticsRequest;
import resources.analytics.requests.GetSubscriptionsAnalyticsRequest;
import types.ChurnCohortsResponse;
import types.CustomerAnalyticsResponse;
import types.LtvResponse;
import types.MrrBreakdownResponse;
import types.NetRevenueResponse;
import types.PaymentAnalyticsResponse;
import types.RevenueAnalyticsResponse;
import types.SubscriptionAnalyticsResponse;

public class RawAnalyticsClient {
  protected final ClientOptions clientOptions;

  public RawAnalyticsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve revenue metrics including total revenue, MRR (monthly recurring revenue), and revenue breakdown by period. Supports filtering by date range and currency.
   */
  public NovabillingApiHttpResponse<RevenueAnalyticsResponse> getRevenue() {
    return getRevenue(GetRevenueAnalyticsRequest.builder().build());
  }

  /**
   * Retrieve revenue metrics including total revenue, MRR (monthly recurring revenue), and revenue breakdown by period. Supports filtering by date range and currency.
   */
  public NovabillingApiHttpResponse<RevenueAnalyticsResponse> getRevenue(
      RequestOptions requestOptions) {
    return getRevenue(GetRevenueAnalyticsRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve revenue metrics including total revenue, MRR (monthly recurring revenue), and revenue breakdown by period. Supports filtering by date range and currency.
   */
  public NovabillingApiHttpResponse<RevenueAnalyticsResponse> getRevenue(
      GetRevenueAnalyticsRequest request) {
    return getRevenue(request,null);
  }

  /**
   * Retrieve revenue metrics including total revenue, MRR (monthly recurring revenue), and revenue breakdown by period. Supports filtering by date range and currency.
   */
  public NovabillingApiHttpResponse<RevenueAnalyticsResponse> getRevenue(
      GetRevenueAnalyticsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/analytics/revenue");if (request.getDateFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
      }
      if (request.getDateTo().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
      }
      if (request.getCurrency().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
      }
      if (request.getGroupBy().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RevenueAnalyticsResponse.class), response);
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (IOException e) {
        throw new NovabillingApiException("Network error executing HTTP request", e);
      }
    }

    /**
     * Retrieve subscription metrics including active count, churn rate, new subscriptions, and status distribution.
     */
    public NovabillingApiHttpResponse<SubscriptionAnalyticsResponse> getSubscriptions() {
      return getSubscriptions(GetSubscriptionsAnalyticsRequest.builder().build());
    }

    /**
     * Retrieve subscription metrics including active count, churn rate, new subscriptions, and status distribution.
     */
    public NovabillingApiHttpResponse<SubscriptionAnalyticsResponse> getSubscriptions(
        RequestOptions requestOptions) {
      return getSubscriptions(GetSubscriptionsAnalyticsRequest.builder().build(),requestOptions);
    }

    /**
     * Retrieve subscription metrics including active count, churn rate, new subscriptions, and status distribution.
     */
    public NovabillingApiHttpResponse<SubscriptionAnalyticsResponse> getSubscriptions(
        GetSubscriptionsAnalyticsRequest request) {
      return getSubscriptions(request,null);
    }

    /**
     * Retrieve subscription metrics including active count, churn rate, new subscriptions, and status distribution.
     */
    public NovabillingApiHttpResponse<SubscriptionAnalyticsResponse> getSubscriptions(
        GetSubscriptionsAnalyticsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/analytics/subscriptions");if (request.getDateFrom().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
        }
        if (request.getDateTo().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
        }
        if (request.getCurrency().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
        }
        if (request.getGroupBy().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubscriptionAnalyticsResponse.class), response);
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (IOException e) {
          throw new NovabillingApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Retrieve customer metrics including total count, new customers, and geographic distribution.
       */
      public NovabillingApiHttpResponse<CustomerAnalyticsResponse> getCustomers() {
        return getCustomers(GetCustomersAnalyticsRequest.builder().build());
      }

      /**
       * Retrieve customer metrics including total count, new customers, and geographic distribution.
       */
      public NovabillingApiHttpResponse<CustomerAnalyticsResponse> getCustomers(
          RequestOptions requestOptions) {
        return getCustomers(GetCustomersAnalyticsRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve customer metrics including total count, new customers, and geographic distribution.
       */
      public NovabillingApiHttpResponse<CustomerAnalyticsResponse> getCustomers(
          GetCustomersAnalyticsRequest request) {
        return getCustomers(request,null);
      }

      /**
       * Retrieve customer metrics including total count, new customers, and geographic distribution.
       */
      public NovabillingApiHttpResponse<CustomerAnalyticsResponse> getCustomers(
          GetCustomersAnalyticsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/analytics/customers");if (request.getDateFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
          }
          if (request.getDateTo().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
          }
          if (request.getCurrency().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
          }
          if (request.getGroupBy().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CustomerAnalyticsResponse.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (IOException e) {
            throw new NovabillingApiException("Network error executing HTTP request", e);
          }
        }

        /**
         * Retrieve payment metrics including success rate, failure rate, total volume, and breakdown by payment provider.
         */
        public NovabillingApiHttpResponse<PaymentAnalyticsResponse> getPayments() {
          return getPayments(GetPaymentsAnalyticsRequest.builder().build());
        }

        /**
         * Retrieve payment metrics including success rate, failure rate, total volume, and breakdown by payment provider.
         */
        public NovabillingApiHttpResponse<PaymentAnalyticsResponse> getPayments(
            RequestOptions requestOptions) {
          return getPayments(GetPaymentsAnalyticsRequest.builder().build(),requestOptions);
        }

        /**
         * Retrieve payment metrics including success rate, failure rate, total volume, and breakdown by payment provider.
         */
        public NovabillingApiHttpResponse<PaymentAnalyticsResponse> getPayments(
            GetPaymentsAnalyticsRequest request) {
          return getPayments(request,null);
        }

        /**
         * Retrieve payment metrics including success rate, failure rate, total volume, and breakdown by payment provider.
         */
        public NovabillingApiHttpResponse<PaymentAnalyticsResponse> getPayments(
            GetPaymentsAnalyticsRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/analytics/payments");if (request.getDateFrom().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
            }
            if (request.getDateTo().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
            }
            if (request.getCurrency().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
            }
            if (request.getGroupBy().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
            }
            if (request.getProvider().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "provider", request.getProvider().get(), false);
            }
            if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentAnalyticsResponse.class), response);
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (IOException e) {
              throw new NovabillingApiException("Network error executing HTTP request", e);
            }
          }

          /**
           * MRR breakdown by movement type (new, expansion, contraction, churn) and by plan.
           */
          public NovabillingApiHttpResponse<MrrBreakdownResponse> getMrrBreakdown() {
            return getMrrBreakdown(GetMrrBreakdownAnalyticsRequest.builder().build());
          }

          /**
           * MRR breakdown by movement type (new, expansion, contraction, churn) and by plan.
           */
          public NovabillingApiHttpResponse<MrrBreakdownResponse> getMrrBreakdown(
              RequestOptions requestOptions) {
            return getMrrBreakdown(GetMrrBreakdownAnalyticsRequest.builder().build(),requestOptions);
          }

          /**
           * MRR breakdown by movement type (new, expansion, contraction, churn) and by plan.
           */
          public NovabillingApiHttpResponse<MrrBreakdownResponse> getMrrBreakdown(
              GetMrrBreakdownAnalyticsRequest request) {
            return getMrrBreakdown(request,null);
          }

          /**
           * MRR breakdown by movement type (new, expansion, contraction, churn) and by plan.
           */
          public NovabillingApiHttpResponse<MrrBreakdownResponse> getMrrBreakdown(
              GetMrrBreakdownAnalyticsRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/analytics/mrr-breakdown");if (request.getDateFrom().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
              }
              if (request.getDateTo().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
              }
              if (request.getCurrency().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
              }
              if (request.getGroupBy().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MrrBreakdownResponse.class), response);
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (IOException e) {
                throw new NovabillingApiException("Network error executing HTTP request", e);
              }
            }

            /**
             * Gross revenue minus refunds and credit notes.
             */
            public NovabillingApiHttpResponse<NetRevenueResponse> getNetRevenue() {
              return getNetRevenue(GetNetRevenueAnalyticsRequest.builder().build());
            }

            /**
             * Gross revenue minus refunds and credit notes.
             */
            public NovabillingApiHttpResponse<NetRevenueResponse> getNetRevenue(
                RequestOptions requestOptions) {
              return getNetRevenue(GetNetRevenueAnalyticsRequest.builder().build(),requestOptions);
            }

            /**
             * Gross revenue minus refunds and credit notes.
             */
            public NovabillingApiHttpResponse<NetRevenueResponse> getNetRevenue(
                GetNetRevenueAnalyticsRequest request) {
              return getNetRevenue(request,null);
            }

            /**
             * Gross revenue minus refunds and credit notes.
             */
            public NovabillingApiHttpResponse<NetRevenueResponse> getNetRevenue(
                GetNetRevenueAnalyticsRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/analytics/net-revenue");if (request.getDateFrom().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
                }
                if (request.getDateTo().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
                }
                if (request.getCurrency().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "currency", request.getCurrency().get(), false);
                }
                if (request.getGroupBy().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "groupBy", request.getGroupBy().get(), false);
                }
                if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, NetRevenueResponse.class), response);
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (IOException e) {
                  throw new NovabillingApiException("Network error executing HTTP request", e);
                }
              }

              /**
               * Monthly cohort retention matrix showing what percentage of each cohort is retained over time.
               */
              public NovabillingApiHttpResponse<ChurnCohortsResponse> getChurnCohorts() {
                return getChurnCohorts(GetChurnCohortsAnalyticsRequest.builder().build());
              }

              /**
               * Monthly cohort retention matrix showing what percentage of each cohort is retained over time.
               */
              public NovabillingApiHttpResponse<ChurnCohortsResponse> getChurnCohorts(
                  RequestOptions requestOptions) {
                return getChurnCohorts(GetChurnCohortsAnalyticsRequest.builder().build(),requestOptions);
              }

              /**
               * Monthly cohort retention matrix showing what percentage of each cohort is retained over time.
               */
              public NovabillingApiHttpResponse<ChurnCohortsResponse> getChurnCohorts(
                  GetChurnCohortsAnalyticsRequest request) {
                return getChurnCohorts(request,null);
              }

              /**
               * Monthly cohort retention matrix showing what percentage of each cohort is retained over time.
               */
              public NovabillingApiHttpResponse<ChurnCohortsResponse> getChurnCohorts(
                  GetChurnCohortsAnalyticsRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("api/analytics/churn-cohorts");if (request.getMonths().isPresent()) {
                    QueryStringMapper.addQueryParameter(httpUrl, "months", request.getMonths().get(), false);
                  }
                  if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ChurnCohortsResponse.class), response);
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (IOException e) {
                    throw new NovabillingApiException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Average customer LTV and lifespan, broken down by plan.
                 */
                public NovabillingApiHttpResponse<LtvResponse> getLifetimeValue() {
                  return getLifetimeValue(null);
                }

                /**
                 * Average customer LTV and lifespan, broken down by plan.
                 */
                public NovabillingApiHttpResponse<LtvResponse> getLifetimeValue(
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("api/analytics/ltv");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("GET", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        return new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LtvResponse.class), response);
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (IOException e) {
                      throw new NovabillingApiException("Network error executing HTTP request", e);
                    }
                  }
                }
