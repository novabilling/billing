/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.invoices;

import com.fasterxml.jackson.core.JsonProcessingException;
import core.ClientOptions;
import core.MediaTypes;
import core.NovabillingApiApiException;
import core.NovabillingApiException;
import core.NovabillingApiHttpResponse;
import core.ObjectMappers;
import core.QueryStringMapper;
import core.RequestOptions;
import errors.BadRequestError;
import errors.NotFoundError;
import errors.UnauthorizedError;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;
import resources.invoices.requests.CreateCheckoutInvoicesRequest;
import resources.invoices.requests.CreateInvoiceDto;
import resources.invoices.requests.FinalizeInvoicesRequest;
import resources.invoices.requests.GetInvoicesRequest;
import resources.invoices.requests.GetPdfInvoicesRequest;
import resources.invoices.requests.ListInvoicesRequest;
import resources.invoices.requests.MarkPaidInvoicesRequest;
import resources.invoices.requests.SendEmailInvoicesRequest;
import resources.invoices.requests.VoidInvoicesRequest;
import types.CheckoutResponse;
import types.InvoiceResponse;
import types.MessageResponse;
import types.PaginatedInvoiceResponse;

public class AsyncRawInvoicesClient {
  protected final ClientOptions clientOptions;

  public AsyncRawInvoicesClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve a paginated list of invoices. Supports filtering by status, customer, and date range.
   */
  public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> list() {
    return list(ListInvoicesRequest.builder().build());
  }

  /**
   * Retrieve a paginated list of invoices. Supports filtering by status, customer, and date range.
   */
  public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> list(
      RequestOptions requestOptions) {
    return list(ListInvoicesRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve a paginated list of invoices. Supports filtering by status, customer, and date range.
   */
  public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> list(
      ListInvoicesRequest request) {
    return list(request,null);
  }

  /**
   * Retrieve a paginated list of invoices. Supports filtering by status, customer, and date range.
   */
  public CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> list(
      ListInvoicesRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("api/invoices");if (request.getStatus().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
      }
      if (request.getCustomerId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "customerId", request.getCustomerId().get(), false);
      }
      if (request.getDateFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
      }
      if (request.getDateTo().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
      }
      if (request.getPage().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<NovabillingApiHttpResponse<PaginatedInvoiceResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaginatedInvoiceResponse.class), response));
              return;
            }
            try {
              if (response.code() == 401) {
                future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (IOException e) {
            future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a draft invoice with line items. The total amount is automatically calculated from the items.
     */
    public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> create(
        CreateInvoiceDto request) {
      return create(request,null);
    }

    /**
     * Create a draft invoice with line items. The total amount is automatically calculated from the items.
     */
    public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> create(
        CreateInvoiceDto request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("api/invoices");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new NovabillingApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceResponse.class), response));
                return;
              }
              try {
                if (response.code() == 404) {
                  future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (IOException e) {
              future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Retrieve detailed invoice information including associated customer, subscription, and payments.
       */
      public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> get(String id) {
        return get(id,GetInvoicesRequest.builder().build());
      }

      /**
       * Retrieve detailed invoice information including associated customer, subscription, and payments.
       */
      public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> get(String id,
          RequestOptions requestOptions) {
        return get(id,GetInvoicesRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve detailed invoice information including associated customer, subscription, and payments.
       */
      public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> get(String id,
          GetInvoicesRequest request) {
        return get(id,request,null);
      }

      /**
       * Retrieve detailed invoice information including associated customer, subscription, and payments.
       */
      public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> get(String id,
          GetInvoicesRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("api/invoices")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceResponse.class), response));
                  return;
                }
                try {
                  if (response.code() == 404) {
                    future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (IOException e) {
                future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Move an invoice from draft to pending status, making it ready for payment.
         */
        public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> finalize(String id) {
          return finalize(id,FinalizeInvoicesRequest.builder().build());
        }

        /**
         * Move an invoice from draft to pending status, making it ready for payment.
         */
        public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> finalize(String id,
            RequestOptions requestOptions) {
          return finalize(id,FinalizeInvoicesRequest.builder().build(),requestOptions);
        }

        /**
         * Move an invoice from draft to pending status, making it ready for payment.
         */
        public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> finalize(String id,
            FinalizeInvoicesRequest request) {
          return finalize(id,request,null);
        }

        /**
         * Move an invoice from draft to pending status, making it ready for payment.
         */
        public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> finalize(String id,
            FinalizeInvoicesRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("api/invoices")
            .addPathSegment(id)
            .addPathSegments("finalize");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", RequestBody.create("", null))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceResponse.class), response));
                    return;
                  }
                  try {
                    if (response.code() == 404) {
                      future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (IOException e) {
                  future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Cancel an unpaid invoice. Paid invoices cannot be voided — use a refund instead.
           */
          public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> void_(String id) {
            return void_(id,VoidInvoicesRequest.builder().build());
          }

          /**
           * Cancel an unpaid invoice. Paid invoices cannot be voided — use a refund instead.
           */
          public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> void_(String id,
              RequestOptions requestOptions) {
            return void_(id,VoidInvoicesRequest.builder().build(),requestOptions);
          }

          /**
           * Cancel an unpaid invoice. Paid invoices cannot be voided — use a refund instead.
           */
          public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> void_(String id,
              VoidInvoicesRequest request) {
            return void_(id,request,null);
          }

          /**
           * Cancel an unpaid invoice. Paid invoices cannot be voided — use a refund instead.
           */
          public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> void_(String id,
              VoidInvoicesRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("api/invoices")
              .addPathSegment(id)
              .addPathSegments("void");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Record an offline or manual payment against an invoice. Accepts an optional paymentMethod (e.g. &quot;cash&quot;, &quot;bank_transfer&quot;, &quot;check&quot;, &quot;manual&quot;).
             */
            public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> markPaid(
                String id) {
              return markPaid(id,MarkPaidInvoicesRequest.builder().build());
            }

            /**
             * Record an offline or manual payment against an invoice. Accepts an optional paymentMethod (e.g. &quot;cash&quot;, &quot;bank_transfer&quot;, &quot;check&quot;, &quot;manual&quot;).
             */
            public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> markPaid(
                String id, RequestOptions requestOptions) {
              return markPaid(id,MarkPaidInvoicesRequest.builder().build(),requestOptions);
            }

            /**
             * Record an offline or manual payment against an invoice. Accepts an optional paymentMethod (e.g. &quot;cash&quot;, &quot;bank_transfer&quot;, &quot;check&quot;, &quot;manual&quot;).
             */
            public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> markPaid(
                String id, MarkPaidInvoicesRequest request) {
              return markPaid(id,request,null);
            }

            /**
             * Record an offline or manual payment against an invoice. Accepts an optional paymentMethod (e.g. &quot;cash&quot;, &quot;bank_transfer&quot;, &quot;check&quot;, &quot;manual&quot;).
             */
            public CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> markPaid(
                String id, MarkPaidInvoicesRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("api/invoices")
                .addPathSegment(id)
                .addPathSegments("mark-paid");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new NovabillingApiException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                CompletableFuture<NovabillingApiHttpResponse<InvoiceResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, InvoiceResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Initiate a payment session with the configured payment provider (Stripe, Paystack, Flutterwave, or M-Pesa). Returns a checkout URL that redirects the customer to the provider's hosted payment page.
               */
              public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
                  String id) {
                return createCheckout(id,CreateCheckoutInvoicesRequest.builder().build());
              }

              /**
               * Initiate a payment session with the configured payment provider (Stripe, Paystack, Flutterwave, or M-Pesa). Returns a checkout URL that redirects the customer to the provider's hosted payment page.
               */
              public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
                  String id, RequestOptions requestOptions) {
                return createCheckout(id,CreateCheckoutInvoicesRequest.builder().build(),requestOptions);
              }

              /**
               * Initiate a payment session with the configured payment provider (Stripe, Paystack, Flutterwave, or M-Pesa). Returns a checkout URL that redirects the customer to the provider's hosted payment page.
               */
              public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
                  String id, CreateCheckoutInvoicesRequest request) {
                return createCheckout(id,request,null);
              }

              /**
               * Initiate a payment session with the configured payment provider (Stripe, Paystack, Flutterwave, or M-Pesa). Returns a checkout URL that redirects the customer to the provider's hosted payment page.
               */
              public CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> createCheckout(
                  String id, CreateCheckoutInvoicesRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("api/invoices")
                  .addPathSegment(id)
                  .addPathSegments("checkout");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new NovabillingApiException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  CompletableFuture<NovabillingApiHttpResponse<CheckoutResponse>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CheckoutResponse.class), response));
                          return;
                        }
                        try {
                          switch (response.code()) {
                            case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Send the invoice to a specified email address, or to the customer's email if none is provided.
                 */
                public CompletableFuture<NovabillingApiHttpResponse<MessageResponse>> sendEmail(
                    String id) {
                  return sendEmail(id,SendEmailInvoicesRequest.builder().build());
                }

                /**
                 * Send the invoice to a specified email address, or to the customer's email if none is provided.
                 */
                public CompletableFuture<NovabillingApiHttpResponse<MessageResponse>> sendEmail(
                    String id, RequestOptions requestOptions) {
                  return sendEmail(id,SendEmailInvoicesRequest.builder().build(),requestOptions);
                }

                /**
                 * Send the invoice to a specified email address, or to the customer's email if none is provided.
                 */
                public CompletableFuture<NovabillingApiHttpResponse<MessageResponse>> sendEmail(
                    String id, SendEmailInvoicesRequest request) {
                  return sendEmail(id,request,null);
                }

                /**
                 * Send the invoice to a specified email address, or to the customer's email if none is provided.
                 */
                public CompletableFuture<NovabillingApiHttpResponse<MessageResponse>> sendEmail(
                    String id, SendEmailInvoicesRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("api/invoices")
                    .addPathSegment(id)
                    .addPathSegments("send-email");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    RequestBody body;
                    try {
                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                    }
                    catch(JsonProcessingException e) {
                      throw new NovabillingApiException("Failed to serialize request", e);
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", body)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Content-Type", "application/json")
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    CompletableFuture<NovabillingApiHttpResponse<MessageResponse>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new NovabillingApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, MessageResponse.class), response));
                            return;
                          }
                          try {
                            switch (response.code()) {
                              case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Returns the PDF binary for the invoice. If a PDF has not been generated yet, it will be created on-demand.
                   */
                  public CompletableFuture<NovabillingApiHttpResponse<Void>> getPdf(String id) {
                    return getPdf(id,GetPdfInvoicesRequest.builder().build());
                  }

                  /**
                   * Returns the PDF binary for the invoice. If a PDF has not been generated yet, it will be created on-demand.
                   */
                  public CompletableFuture<NovabillingApiHttpResponse<Void>> getPdf(String id,
                      RequestOptions requestOptions) {
                    return getPdf(id,GetPdfInvoicesRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Returns the PDF binary for the invoice. If a PDF has not been generated yet, it will be created on-demand.
                   */
                  public CompletableFuture<NovabillingApiHttpResponse<Void>> getPdf(String id,
                      GetPdfInvoicesRequest request) {
                    return getPdf(id,request,null);
                  }

                  /**
                   * Returns the PDF binary for the invoice. If a PDF has not been generated yet, it will be created on-demand.
                   */
                  public CompletableFuture<NovabillingApiHttpResponse<Void>> getPdf(String id,
                      GetPdfInvoicesRequest request, RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                      .addPathSegments("api/invoices")
                      .addPathSegment(id)
                      .addPathSegments("pdf");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      Request.Builder _requestBuilder = new Request.Builder()
                        .url(httpUrl.build())
                        .method("GET", null)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Accept", "application/json");
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      CompletableFuture<NovabillingApiHttpResponse<Void>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            if (response.isSuccessful()) {
                              future.complete(new NovabillingApiHttpResponse<>(null, response));
                              return;
                            }
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            try {
                              if (response.code() == 404) {
                                future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new NovabillingApiApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new NovabillingApiException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }
                  }
