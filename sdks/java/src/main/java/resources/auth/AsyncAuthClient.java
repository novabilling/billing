/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.auth;

import core.ClientOptions;
import core.RequestOptions;
import java.util.concurrent.CompletableFuture;
import resources.auth.requests.ForgotPasswordDto;
import resources.auth.requests.LoginDto;
import resources.auth.requests.RefreshTokenDto;
import resources.auth.requests.RegisterDto;
import resources.auth.requests.ResetPasswordDto;
import types.LoginResponse;
import types.MessageResponse;
import types.RegisterResponse;
import types.TokenPairResponse;

public class AsyncAuthClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawAuthClient rawClient;

  public AsyncAuthClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawAuthClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawAuthClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Create a new tenant account with a company name. This provisions an isolated database, generates an API key, and returns JWT tokens.
   */
  public CompletableFuture<RegisterResponse> register(RegisterDto request) {
    return this.rawClient.register(request).thenApply(response -> response.body());
  }

  /**
   * Create a new tenant account with a company name. This provisions an isolated database, generates an API key, and returns JWT tokens.
   */
  public CompletableFuture<RegisterResponse> register(RegisterDto request,
      RequestOptions requestOptions) {
    return this.rawClient.register(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Authenticate with email and password. Returns an access token and refresh token.
   */
  public CompletableFuture<LoginResponse> login(LoginDto request) {
    return this.rawClient.login(request).thenApply(response -> response.body());
  }

  /**
   * Authenticate with email and password. Returns an access token and refresh token.
   */
  public CompletableFuture<LoginResponse> login(LoginDto request, RequestOptions requestOptions) {
    return this.rawClient.login(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Exchange a valid refresh token for a new access/refresh token pair.
   */
  public CompletableFuture<TokenPairResponse> refreshToken(RefreshTokenDto request) {
    return this.rawClient.refreshToken(request).thenApply(response -> response.body());
  }

  /**
   * Exchange a valid refresh token for a new access/refresh token pair.
   */
  public CompletableFuture<TokenPairResponse> refreshToken(RefreshTokenDto request,
      RequestOptions requestOptions) {
    return this.rawClient.refreshToken(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Send a password reset email to the specified address. Always returns success to prevent email enumeration.
   */
  public CompletableFuture<MessageResponse> forgotPassword(ForgotPasswordDto request) {
    return this.rawClient.forgotPassword(request).thenApply(response -> response.body());
  }

  /**
   * Send a password reset email to the specified address. Always returns success to prevent email enumeration.
   */
  public CompletableFuture<MessageResponse> forgotPassword(ForgotPasswordDto request,
      RequestOptions requestOptions) {
    return this.rawClient.forgotPassword(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Set a new password using the token received via email.
   */
  public CompletableFuture<MessageResponse> resetPassword(ResetPasswordDto request) {
    return this.rawClient.resetPassword(request).thenApply(response -> response.body());
  }

  /**
   * Set a new password using the token received via email.
   */
  public CompletableFuture<MessageResponse> resetPassword(ResetPasswordDto request,
      RequestOptions requestOptions) {
    return this.rawClient.resetPassword(request, requestOptions).thenApply(response -> response.body());
  }
}
