/**
 * This file was auto-generated by Fern from our API Definition.
 */

package resources.auth;

import core.ClientOptions;
import core.RequestOptions;
import resources.auth.requests.ForgotPasswordDto;
import resources.auth.requests.LoginDto;
import resources.auth.requests.RefreshTokenDto;
import resources.auth.requests.RegisterDto;
import resources.auth.requests.ResetPasswordDto;
import types.LoginResponse;
import types.MessageResponse;
import types.RegisterResponse;
import types.TokenPairResponse;

public class AuthClient {
  protected final ClientOptions clientOptions;

  private final RawAuthClient rawClient;

  public AuthClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawAuthClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawAuthClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Create a new tenant account with a company name. This provisions an isolated database, generates an API key, and returns JWT tokens.
   */
  public RegisterResponse register(RegisterDto request) {
    return this.rawClient.register(request).body();
  }

  /**
   * Create a new tenant account with a company name. This provisions an isolated database, generates an API key, and returns JWT tokens.
   */
  public RegisterResponse register(RegisterDto request, RequestOptions requestOptions) {
    return this.rawClient.register(request, requestOptions).body();
  }

  /**
   * Authenticate with email and password. Returns an access token and refresh token.
   */
  public LoginResponse login(LoginDto request) {
    return this.rawClient.login(request).body();
  }

  /**
   * Authenticate with email and password. Returns an access token and refresh token.
   */
  public LoginResponse login(LoginDto request, RequestOptions requestOptions) {
    return this.rawClient.login(request, requestOptions).body();
  }

  /**
   * Exchange a valid refresh token for a new access/refresh token pair.
   */
  public TokenPairResponse refreshToken(RefreshTokenDto request) {
    return this.rawClient.refreshToken(request).body();
  }

  /**
   * Exchange a valid refresh token for a new access/refresh token pair.
   */
  public TokenPairResponse refreshToken(RefreshTokenDto request, RequestOptions requestOptions) {
    return this.rawClient.refreshToken(request, requestOptions).body();
  }

  /**
   * Send a password reset email to the specified address. Always returns success to prevent email enumeration.
   */
  public MessageResponse forgotPassword(ForgotPasswordDto request) {
    return this.rawClient.forgotPassword(request).body();
  }

  /**
   * Send a password reset email to the specified address. Always returns success to prevent email enumeration.
   */
  public MessageResponse forgotPassword(ForgotPasswordDto request, RequestOptions requestOptions) {
    return this.rawClient.forgotPassword(request, requestOptions).body();
  }

  /**
   * Set a new password using the token received via email.
   */
  public MessageResponse resetPassword(ResetPasswordDto request) {
    return this.rawClient.resetPassword(request).body();
  }

  /**
   * Set a new password using the token received via email.
   */
  public MessageResponse resetPassword(ResetPasswordDto request, RequestOptions requestOptions) {
    return this.rawClient.resetPassword(request, requestOptions).body();
  }
}
